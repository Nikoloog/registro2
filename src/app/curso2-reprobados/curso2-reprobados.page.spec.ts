import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Curso2ReprobadosPage } from './curso2-reprobados.page';

describe('Curso2ReprobadosPage', () => {
  let component: Curso2ReprobadosPage;
  let fixture: ComponentFixture<Curso2ReprobadosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Curso2ReprobadosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
