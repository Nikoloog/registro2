import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Curso4ReprobadosPage } from './curso4-reprobados.page';

describe('Curso4ReprobadosPage', () => {
  let component: Curso4ReprobadosPage;
  let fixture: ComponentFixture<Curso4ReprobadosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Curso4ReprobadosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
