import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Curso3ReprobadosPage } from './curso3-reprobados.page';

describe('Curso3ReprobadosPage', () => {
  let component: Curso3ReprobadosPage;
  let fixture: ComponentFixture<Curso3ReprobadosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Curso3ReprobadosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
